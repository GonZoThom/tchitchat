import React from 'react';

const List = () => {
    // Create or Join a Room (Create can be another component)
    return (
        <div>
            <h2>Future List</h2>
        </div>
    )
};

export default List;